# Telegram (required)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Anthropic (only used by discovery.ts when regex detectors miss)
ANTHROPIC_API_KEY=

# GitHub (only used by bot.ts to push companies.json back to the repo)
GITHUB_TOKEN=
GITHUB_REPO=your-username/job-watcher

# Optional: SerpAPI for Indeed/Google Jobs/LinkedIn (Phase 5, paid)
SERPAPI_KEY=
